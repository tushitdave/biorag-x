# BioRAG-X backend (adaptive-biomed-rag)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pandas>=2.0
pyarrow>=14.0
numpy>=1.26
rank-bm25>=0.2.2
scikit-learn>=1.3
python-dotenv>=1.0
pyyaml>=6.0
openai>=1.13

# Models and indexes (MedCPT encoders, cross-encoder, FAISS, PageIndex clustering).
# On Apple Silicon install torch and faiss from conda-forge instead of pip: pip builds
# of torch and faiss each bundle their own OpenMP runtime and crash when loaded together.
torch>=2.2
transformers>=4.38
faiss-cpu>=1.7
scipy>=1.11
